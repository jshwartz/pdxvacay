
<div class="panel panel-default">
  <div class="panel-heading">
    <h2 class="panel-title">{{model.title}}
        {{#link-to 'admin.activities.list'}}
        <button type="button" class="btn btn-danger btn-xs flt-right">
          <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
        </button>
      {{/link-to}}
    </h2>
  </div>
  <div class="panel-body">

    {{outlet}}
  </div>
</div>
