<div class="container">
  <div class="page-header">
    <div class="row">
      <div class="col-lg-4 col-lg-offset-4 col-sm-4 col-sm-offset-4 text-center">
        <h1>Lists</h1>
        <p>All of our favorite things to do, organzied a bit</p>
      </div>
    </div>
  </div>
  <div class="list-container">
      {{#each model as |list|}}
        {{list-box list=list}}
      {{/each}}
  </div>
</div>
